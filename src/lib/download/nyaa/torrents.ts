import { SearchTorrent } from "@/lib/download/nyaa/api/types"

export async function filterTorrentEpisodes(props: {
    torrents: SearchTorrent[],
    episode: number,
}) {

}

export async function filterTorrentBatches(props: {
    torrents: SearchTorrent[],
    season: number,
}) {

}
